// Base URL of the deployed Fridge Organiser backend (FastAPI on Render).
// Kept in one place so the embedded copy of the app is easy to repoint.
export const API_BASE = "https://fridge-organiser.onrender.com"
